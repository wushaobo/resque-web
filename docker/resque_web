server {
  listen 80;
  server_name _;

  root /opt/apps/resque-web/test/dummy/public;

  passenger_enabled on;
  passenger_ruby /usr/local/bin/ruby;
  passenger_app_env production;
  passenger_friendly_error_pages on;
}